name: go-test

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  call-workflow-passing-data:
    uses: cyverse-de/github-workflows/.github/workflows/go-test.yml@v0.4.1
    with:
      go-version: "1.25"
